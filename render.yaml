services:
  - type: web
    name: customer-segmentation-api
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: LOG_LEVEL
        value: info
      - key: MODELS_DIR
        value: models
